# Undead Survivor 开发约定

- 本项目为独立仓库，改动限制在项目目录内。
- 玩家在有限场地内使用 WASD 自由移动，Pointer Lock 鼠标控制水平自由转向；准星固定在屏幕中心。
- 北侧与东侧各有三个固定刷怪点，不在玩家当前背后或 8 米内刷新；无可用点时保留本波配额，等待可用点，不积攒突发补刷。
- 玩家初始生命 100，僵尸接触后播放攻击动作并造成伤害，血量归零失败；玩家走入河流或在水中落地立即失败。
- 空格单次跳跃，禁止空中连跳；河道视觉、落水判定与僵尸寻路共用地形数据。僵尸只能从两座桥过河，玩家可以跳河。
- 每波数量在第 1～6、7～8、9～11、12 波以后分别每波增加 6、5、4、3 只；基础移速从 1.4 m/s 每波增加 0.15 m/s，达到 2.5 m/s 后封顶。刷完并击杀整波后全员回满 100 HP，休整 5 秒进入下一波。
- 瞄准和弹道必须共享目标点，开火时从枪口检测遮挡；不要只旋转装饰枪模。
- 场景为原创低多边形风格，不导入《未转变者》的模型或贴图。
- Windows PowerShell 读取中文源文件必须使用 `Get-Content -Encoding utf8`。
- Git 提交格式为 `<type>: <简体中文具体摘要>`，类型使用英文小写，末尾不加句号。每次提交只含一项逻辑相关改动。
- 存在远程仓库时，必要验证通过后提交并推送本次相关文件；不包含用户无关改动，不强制推送或改写历史。没有远程或推送受阻时说明原因。
- 浏览器验收保持单 worker，不同时运行多份 3D 游戏测试；标题页与暂停页必须按需绘制，后台停止绘制。开发端口 5176，测试端口 5177，测试不复用已有服务。
- 正式模式的刷新率从每秒 1 只开始，每波增加 0.1 只，最多每秒 2 只；移动和刷新使用有效游玩时间，暂停与后台不能增加坚守成绩。
- 练习模式的僵尸保持静止、击倒复位，落水也失败但不写榜；正式模式失败按已清完波数写入本机排行榜，同波数比较击杀、再比较时长。波次榜使用独立存储键，不混入旧时长榜。
- 尸群使用实例化绘制并限制同时存在的数量；不要为每只僵尸创建独立材质和场景节点树。
- 默认只修改源码、网页版并验证；只有用户明确要求重新打包时才更新 portable EXE，不能在普通功能改动后自动运行 dist:portable。
- Steam 双人模式由房主模拟波次、僵尸、两名玩家的伤害和队友移动；客户端仅发送输入，不能提交位置、血量或击杀结果。
- 双人模式单人死亡后观战，两人都死亡才结算；联机结果不混入单人排行榜。菜单和后台不暂停联机模拟，后台仅执行逻辑、不绘制场景。
- 双人端到端验收允许同一测试内开启两个客户端；仍使用单 worker，不并行运行其他 3D 测试。本地通道验证不等于两账号 Steam 联网验收。
- 自动验证必须静默进行：Electron 使用 `--silent` 隐藏、不可聚焦、不占任务栏且静音；浏览器使用 headless 与静音。禁止在用户工作时启动可见游戏窗口或对可见窗口执行 show/focus/bringToFront。用户明确要求前台试玩时才可例外。
- 0.5.0 及更早的便携版不支持静默参数，禁止自动启动；`test:portable` 只验证 0.6.0 起的当前版本成品，必须保持隐藏、静音并检查窗口状态。用户明确要求打包时，才生成新版本 EXE。
